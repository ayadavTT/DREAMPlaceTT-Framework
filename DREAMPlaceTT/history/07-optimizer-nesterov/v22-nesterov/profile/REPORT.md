# v22-nesterov — measured run (real silicon, 2026-06-03)

From the isolated harness:
```
Usage: ./host/build/v22_nesterov_microbench --inputs <path.bin> --output <path.bin>
```

Full stdout: `run.log`. Per-zone Tracy pending (ENABLE_TRACY=ON; needs dump_profiler on clean device).
