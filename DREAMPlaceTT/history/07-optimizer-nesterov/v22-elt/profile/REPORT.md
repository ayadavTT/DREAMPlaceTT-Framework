# v22-elt — measured run (real silicon, 2026-06-03)

From the isolated harness:
```
Usage: ./host/build/v22_elt_microbench --op clamp|combine|precond --inputs <p> --output <p>
```

Full stdout: `run.log`. Per-zone Tracy pending (ENABLE_TRACY=ON; needs dump_profiler on clean device).
