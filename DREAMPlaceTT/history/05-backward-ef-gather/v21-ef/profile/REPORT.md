# v21-ef — measured run (real silicon, 2026-06-03)

From the isolated harness:
```
Usage: ./host/build/v21_ef_microbench --inputs <path.bin> --output <path.bin> [--mode sfpu|scalar|--v5]
```

Full stdout: `run.log`. Per-zone Tracy pending (ENABLE_TRACY=ON; needs dump_profiler on clean device).
