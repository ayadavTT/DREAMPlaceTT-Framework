# v31-backward — measured run (real silicon, 2026-06-03)

Ran but no parseable result (wedged device, or needs file-based --inputs — see run.log).

Full stdout: `run.log`. Per-zone Tracy pending (ENABLE_TRACY=ON; needs dump_profiler on clean device).
